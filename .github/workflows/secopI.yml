name: SECOP Automatización

on:
  schedule:
    - cron: "0 12 * * *"   # ⏰ Todos los días 7:00 AM Colombia (UTC-5)
  workflow_dispatch:       # ▶️ Ejecutar manualmente

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      # 1. Clonar repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2. Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 3. Instalar dependencias
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Ejecutar script
      - name: Run script
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          python script.py
